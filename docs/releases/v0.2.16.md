# AgSwarm v0.2.16

## Auto-Update

- Fixes the macOS updater asset upload.
- Publishes both `AgSwarm-macOS-Apple-Silicon.app.tar.gz` and `AgSwarm-macOS-Apple-Silicon.app.tar.gz.sig` so `latest.json` points to a reachable signed update bundle.

## Verification

- Release validation must confirm that every URL in `latest.json` returns HTTP 200.
- Release validation must confirm that macOS and Windows updater signatures are embedded as signature content, not signature URLs.
