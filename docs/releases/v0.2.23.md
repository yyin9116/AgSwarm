# AgSwarm Client v0.2.23

This release carries forward the Windows installer fix from v0.2.22 and repairs the release CI preflight.

## Fixed

- Keeps Linux CI as a Rust-only compatibility check while still satisfying Tauri's resource path validation with a small skipped-runtime placeholder.
- Preserves full bundled Ag/pi-web runtime archive generation for supported release targets: Windows x64 and macOS Apple Silicon.

## Verification

- `npm run lint`
- `npm run build`
- `npm run check:sidecars -- --target aarch64-apple-darwin`
- `cargo check`
