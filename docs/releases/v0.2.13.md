# AgSwarm v0.2.13

## Release Packaging
- Fixed Windows release artifact collection when Tauri emits bundles under the default `target/release/bundle` directory.
- Keeps the short product-first asset names introduced in the previous release attempt.

## Included Fixes
- Includes the packaged pi-web startup fix for Windows installs.
- Suppresses extra Windows console windows when bundled runtime child processes start.

## Distribution
- GitHub Actions builds macOS Apple Silicon and Windows x64 release artifacts from the `v0.2.13` tag.
